import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Contact Page',
    description: 'contact page description',
    keywords: ['contact', 'monica paladinez'],
};

export default function ContactPage() {
    return (
        <div>
            <h1>ContactPage</h1>
        </div>
    );
}
